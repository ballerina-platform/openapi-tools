openapi: 3.0.0
info:
  title: Shopify Admin API
  version: "2021-10"
tags:
  - name: customers
  - name: products
  - name: orders
  - name: fulfillments
  - name: order_risks
paths:
  /admin:
    post:
      operationId: "test1"
      requestBody:
        content:
          "application/json":
            schema:
              $ref: "#/components/schemas/Person"
      responses:
        200:
          description: Status OK

components:
  schemas:
    Hobby:
      type: array
      items:
          type: string
          maxLength: 23
      maxItems: 5
      minItems: 2
    Person:
      type: object
      required:
        - id
      properties:
        hobby:
          $ref: '#/components/schemas/Hobby' #reference array
        Details:
          type: array #inline array
          items:
            type: string
            minLength: 7
          maxItems: 5
        id:
          type: integer
        fee:
          type: array
          items:
            type: number     #number format
            format: float
            maximum: 445.4
        limit:
          description: The maximum number of items in the response (as set in the query or by default).
          items:
            type: integer
            maximum: 67