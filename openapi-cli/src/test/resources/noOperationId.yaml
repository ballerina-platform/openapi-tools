openapi: 3.0.1
info:
  title: serviceName
  version: 1.0.0
servers:
  - url: http://petstore.openapi.io
paths:
  /user:
   get:
    responses:
        200:
          description: Successful
          content:
            application/json:
              example: Ok
   post:
    responses:
        200:
          description: Successful
          content:
            application/json:
              example: Ok
