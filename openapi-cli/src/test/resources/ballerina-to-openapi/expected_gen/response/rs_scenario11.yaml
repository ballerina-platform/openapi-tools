openapi: 3.0.1
info:
  title: ' payloadV'
  version: 1.0.0
servers:
  - url: "{server}:{port}/payloadV"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /hi:
    post:
      operationId: operation_post_/hi
      responses:
        "200":
          description: Ok
          content:
            text/plain:
              schema:
                type: string
components: {}
