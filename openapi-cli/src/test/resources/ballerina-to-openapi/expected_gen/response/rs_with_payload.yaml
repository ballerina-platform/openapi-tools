openapi: 3.0.1
info:
  title: PayloadV
  version: 0.0.0
servers:
  - url: "{server}:{port}/payloadV"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /pet01:
    get:
      operationId: operation_get_/pet01
      responses:
        "200":
          description: Ok
          content:
            application/xml:
              schema:
                $ref: '#/components/schemas/User'
  /pet02:
    get:
      operationId: operation_get_/pet02
      responses:
        "200":
          description: Ok
          content:
            application/xml:
              schema:
                type: object
  /pet03:
    get:
      operationId: operation_get_/pet03
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                type: string
  /:
    get:
      operationId: operation_get
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
            application/test+json:
              schema:
                $ref: '#/components/schemas/User'
components:
  schemas:
    User:
      required:
        - id
        - name
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string
