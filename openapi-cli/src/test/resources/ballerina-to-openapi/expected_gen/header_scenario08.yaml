paths:
  /ping:
    get:
      operationId: operation_get_/ping
      parameters:
        - name: x-client
          in: header
          schema:
            type: string
            nullable: true
            default: header1
      responses:
        "200":
          description: Ok
