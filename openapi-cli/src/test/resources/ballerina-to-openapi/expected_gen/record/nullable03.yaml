components:
  schemas:
    Pet:
      required:
        - id
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string
          nullable: true
      nullable: true
    Link:
      required:
        - rel
      type: object
      properties:
        rel:
          type: string
          nullable: true
        pet:
          $ref: '#/components/schemas/Pet'
