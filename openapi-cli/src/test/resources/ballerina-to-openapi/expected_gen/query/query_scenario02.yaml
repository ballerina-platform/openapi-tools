paths:
  /pets:
    get:
      operationId: getPets
      parameters:
        - name: offset
          in: query
          schema:
            type: integer
            format: int64
            nullable: true
      responses:
        "200":
          description: Ok
