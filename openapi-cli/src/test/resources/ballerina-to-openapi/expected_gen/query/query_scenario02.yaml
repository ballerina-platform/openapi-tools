paths:
  /pets:
    get:
      operationId: operation_get_/pets
      parameters:
        - name: offset
          in: query
          schema:
            type: integer
            format: int32
            nullable: true
      responses:
        "200":
          description: Ok
