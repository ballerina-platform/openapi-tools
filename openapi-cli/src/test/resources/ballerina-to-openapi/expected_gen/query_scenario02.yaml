openapi: 3.0.1
info:
  title: ' payloadV'
  version: 1.0.0
servers:
  - url: "{server}:{port}/payloadV"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /pets:
    get:
      operationId: operation_get_/pets
      parameters:
        - name: offset
          in: query
          required: false
          schema:
            type: object
      responses:
        "200":
          description: Ok
components: {}
