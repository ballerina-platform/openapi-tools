openapi: 3.0.1
info:
  title: Mock API Doc
  version: 0.1.0
servers:
  - url: "{server}:{port}/title"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /greeting:
    get:
      operationId: operation_get_/greeting
      responses:
        "200":
          description: Ok
          content:
            text/plain:
              schema:
                type: string
components: {}
