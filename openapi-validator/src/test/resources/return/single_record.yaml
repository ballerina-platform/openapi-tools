openapi: 3.0.1
info:
  title: '''limit'
  version: 0.1.0
servers:
  - url: "{server}:{port}/"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /:
    get:
      operationId: operation_get
      responses:
        "202":
          description: Accepted
          content:
            application/json:
              schema: {}
components: {}
