openapi: 3.0.1
info:
  title: V4
  version: 0.1.0
servers:
  - url: "{server}:{port}/v4"
    variables:
      server:
        default: http://localhost
      port:
        default: "9090"
paths:
  /pet:
    post:
      operationId: operation_get
      responses:
        "202":
          description: Accepted
components: {}
